<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
  xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <groupId>net.hwongu</groupId>
  <artifactId>AppDatabase</artifactId>
  <version>1.0-SNAPSHOT</version>
  <packaging>jar</packaging>

  <name>AppDatabase</name>
  <url>http://maven.apache.org</url>

  <properties>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <java.version>17</java.version>
    <flyway.version>10.19.0</flyway.version>
    <postgresql.driver.version>42.7.4</postgresql.driver.version>
    <database.driver>org.postgresql.Driver</database.driver>
    <!-- <database.url>jdbc:postgresql://localhost:5432/prueba</database.url> -->
    <!-- <database.username>postgres</database.username> -->
    <!-- <database.password>clave</database.password> -->
    <database.url>jdbc:postgresql://${DemoDb.Url}/${DemoDb.DB}</database.url>
    <database.username>${DemoDb.User}</database.username>
    <database.password>${DemoDb.Password}</database.password>
  </properties>

  <dependencies>
    <dependency>
      <groupId>org.flywaydb</groupId>
      <artifactId>flyway-core</artifactId>
      <version>${flyway.version}</version>
    </dependency>

    <dependency>
      <groupId>org.flywaydb</groupId>
      <artifactId>flyway-database-postgresql</artifactId>
      <version>${flyway.version}</version>
    </dependency>

    <dependency>
      <groupId>org.postgresql</groupId>
      <artifactId>postgresql</artifactId>
      <version>${postgresql.driver.version}</version>
      <scope>runtime</scope>
    </dependency>
  </dependencies>


  <build>
    <plugins>
      <plugin>
        <groupId>org.flywaydb</groupId>
        <artifactId>flyway-maven-plugin</artifactId>
        <version>${flyway.version}</version>
        <configuration>
          <driver>${database.driver}</driver>
          <url>${database.url}</url>
          <user>${database.username}</user>
          <password>${database.password}</password>
          <locations>
            <location>filesystem:src/main/resources/migrations</location>
          </locations>
          <cleanDisabled>false</cleanDisabled>
          <encoding>ISO-8859-1</encoding>
          <cleanOnValidationError>true</cleanOnValidationError>
          <placeholders>
            <email_type.primary>hwongu@gmail.com</email_type.primary>
            <email_type.work>hwong@gmail.com</email_type.work>
          </placeholders>
        </configuration>
        <executions>
          <execution>
            <id>flyway database migration</id>
            <phase>pre-integration-test</phase>
            <goals>
              <goal>clean</goal>
              <goal>baseline</goal>
              <goal>migrate</goal>
              <goal>info</goal>
            </goals>
          </execution>
        </executions>
      </plugin>
    </plugins>
  </build>


</project>
